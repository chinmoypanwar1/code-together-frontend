function Home() {
  // Try to get the user Details.
  // If the accessToken fails try to refresh
  // If the refreshToken fail as well.
  // Show the home page.
  // Else if any of the above works redirect the user to dashboard.
  return (
    <>

    </>
  )
}

export default Home
